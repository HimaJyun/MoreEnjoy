main: "jp.jyn.moreenjoy.joinmessage.Main"
name: "JoinMessage"
version: "${project.version}"
website: "${project.parent.url}"
author: "HimaJyun( https://jyn.jp/ )"
description: "${project.description}"
api-version: "${spigot.api-version}"

commands:
  moreenjoy-joinmessage:
    description: "reload config."
    usage: "/<command> reload"
    permission: "moreenjoy.joinmessage.reload"

permissions:
  moreenjoy.joinmessage.reload:
    default: op
